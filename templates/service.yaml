apiVersion: v1
kind: Service
metadata:
  name: p-service
  labels:
    app: portfolioo141
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: portfolioo141