apiVersion: v1
kind: Service
metadata:
  name: u-service
  namespace: analytics
  labels:
    app: umami-web
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: umami-web